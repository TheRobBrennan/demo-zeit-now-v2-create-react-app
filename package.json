{
  "name": "demo-zeit-now-v2-create-react-app",
  "version": "0.1.0",
  "author": "Rob Brennan <rob@therobbrennan.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TheRobBrennan/demo-zeit-now-v2-create-react-app"
  },
  "private": true,
  "dependencies": {
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "react-scripts": "2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "now-build": "react-scripts build && mv build dist",
    "deploy": "now && now alias"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
